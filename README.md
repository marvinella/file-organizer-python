# file-organizer-python
a simple and efficient Python script that organizes files into folders based on their extensions using a dictionary-based mapping
It scans all files using os.listdir.
It matches extensions using a dictionary structure.
It physically moves files using the shutil library.
